#include <stdio.h>
/**
 * main -this betty is annoying
 *
 * Return: she is annoying ohhhhh
 */
int main(void)
{
	char ReeChard, ReeKyboo;

	for (ReeChard = 'a'; ReeChard <= 'z'; ++ReeChard)
		putchar(ReeChard);
	for (ReeKyboo = 'A'; ReeKyboo <= 'Z'; ++ReeKyboo)
		putchar(ReeKyboo);
	putchar('\n');
	return (0);
}
