#include <stdio.h>
/**
 * main -this betty is annoying
 *
 * Return: she is annoying ohhhhh
 */
int main(void)
{
	char ReeChard;

	for (ReeChard = 'z'; ReeChard >= 'a'; --ReeChard)
		putchar(ReeChard);
	putchar('\n');
	return (0);
}
