#include "main.h"
/**
 * _isdigit - This checks if the character is an uppercase
 *
 * @c: Character to be checked
 *
 * Return: 1 if c is a digit else returns 0
 *
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
