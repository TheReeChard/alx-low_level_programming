#include "main.h"

/**
 * _islower - this checks for lowercase character
 *
 * @c: i am angry at betty
 * Return: 1 if c is lowercase, else 0
 *
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
