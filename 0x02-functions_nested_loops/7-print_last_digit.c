#include "main.h"
/**
 * print_last_digit - will print modulo 10
 * @n: meh
 * Return: value of the last digit
 */
int print_last_digit(int n)
{
	int i;

	if (n < 0)
		n = -n;

	i = n % 10;

	if (a < 0)
		a = -a;

	_putchar(i + '0');

	return (i);
}
